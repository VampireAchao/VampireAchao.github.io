---
title: 箭头函数和解构赋值混用
date: 2021-10-25 18:43:48
tags: 前端
---

> 一个人的礼貌是一面照出他的肖像的镜子。——歌德

我们在前端对数组进行操作时，如果使用`map`函数，编写的箭头函数其实也是可以解构的

例如经常遇到的，将一个对象数组中的属性，更换另一个属性名，用于给`vue`组件传值

写法如下：

```javascript
[{name:'achao',id:1},{name:'阿超',id:2}].map(({name:username,id:userId})=>({username,userId}))
```

![image-20211025183250288](/imgs/oss/picGo/image-20211025183250288.png)

注意的是，结构赋值需要打括号，下方再举一个例子，这里用`flatMap`收集全部的`name`和`id`直接变为一个数组

```javascript
[{name:'achao',id:1},{name:'阿超',id:2}].flatMap(({name,id})=>[name,id])
```



![image-20211025183338374](/imgs/oss/picGo/image-20211025183338374.png)

